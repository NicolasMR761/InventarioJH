# Inventario JH - Offline (MVP)

## Ejecutar
1) Activar venv:
   PowerShell:
   .\.venv\Scripts\Activate.ps1

2) Instalar deps:
   python -m pip install -r requirements.txt

3) Correr:
   python -m app.main

La base de datos se crea en:
app_data/inventario.db
